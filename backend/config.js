const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "",
        database: "bdgestion",
        port: 3306,
        connectTimeout: 60000
    },
}

module.exports = config;
